;; ==============================================================================
;;
;; @@@@@@@@@@@@@@@@@@@
;; @ Exercise 1.3[*] @
;; @@@@@@@@@@@@@@@@@@@
;;
;; the set T of natural numbers where:
;;   1) 0 in T, and
;;   2) if n in T, then n+3 in T, and
;;   3) T != S
;; could be equal to N = {0,1,2,3,...}
;; since we are no longer constrained
;; to the smallest set satisfying these
;; properties.
